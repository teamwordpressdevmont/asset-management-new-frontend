import AssetHistory from "@/components/dashboard/assets/AssetHistory";

export const metadata = { title: "Histoty — Asset." };

export default function AssetHistoryPage() {
  return <AssetHistory />;
}
